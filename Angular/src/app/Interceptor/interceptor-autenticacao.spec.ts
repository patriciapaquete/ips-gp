import { InterceptorAutenticacao } from './interceptor-autenticacao';

describe('InterceptorAutenticacao', () => {
  it('should create an instance', () => {
    expect(new InterceptorAutenticacao()).toBeTruthy();
  });
});
